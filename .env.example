# Public — shipped in the client bundle. Nothing secret goes in this file.
# Copy to .env.local (gitignored) and fill in real values from your Supabase
# project settings once one exists; local dev can point at the local stack
# started with `npx supabase start` (it prints ANON_KEY / API_URL).

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=replace-with-local-or-project-anon-key
VITE_APP_BASE_URL=http://localhost:5173
VITE_TURNSTILE_SITE_KEY=replace-with-turnstile-site-key

# The public half of the VAPID pair (npx web-push generate-vapid-keys). Public
# by design — it is what identifies this app to the push services. Leave it
# empty and the notifications switch reports itself unavailable rather than
# failing at the moment somebody presses it.
VITE_VAPID_PUBLIC_KEY=

# ---------------------------------------------------------------------------
# Secrets — NOT here, and not with a VITE_ prefix.
#
# Anything Vite sees as VITE_* is compiled into the browser bundle, where it
# is readable by anyone. These belong to Edge Functions and are set with
# `npx supabase secrets set NAME=value` (or the dashboard's Edge Function
# secrets), never in this file:
#
#   TURNSTILE_SECRET_KEY   verify-turnstile — the server half of the captcha
#   RESEND_API_KEY         send-email — the key it posts to Resend with
#   RESEND_FROM            e.g. "CovertCook <dinner@yourdomain>"; the domain
#                          has to be verified with Resend first, or delivery
#                          silently goes to spam
#   VAPID_PRIVATE_KEY      the other half of the pair. Rotating it kills every
#                          existing subscription silently — every phone stays
#                          subscribed to a key that no longer signs anything.
#   VAPID_PUBLIC_KEY       the same value as VITE_VAPID_PUBLIC_KEY above
#   VAPID_SUBJECT          mailto: or https: contact; the push services require
#                          one and reject the request without it
#   SEND_EMAIL_HOOK_SECRET the `v1,whsec_...` string the dashboard generates
#                          when the Send Email Hook is enabled. Without it,
#                          send-email refuses every call — it is deployed
#                          without a JWT gate, so the signature is the only
#                          thing keeping it from being an open relay.
#
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are injected into Edge Functions
# automatically — never set them as function secrets by hand.
#
# They DO belong in .env.local, for the admin scripts that run on your own
# machine (scripts/seed-testers.mjs). Node does not read this file the way Vite
# does, so the script reads it itself. Two things to get right:
#
#   * the API URL, not the database URL. `npx supabase status` prints both;
#     API_URL (http://127.0.0.1:54321) is the one, DB_URL (…:54322) is psql's.
#   * which project. The script refuses anything but localhost unless you pass
#     --production, because eight accounts with a published password are a way
#     in that nobody remembers to shut.
#
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_SERVICE_ROLE_KEY=the service_role key from `npx supabase status`
